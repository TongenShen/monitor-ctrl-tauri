# 默认按文本处理并统一换行, 避免 Windows/Linux 协作时出现整文件 diff
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制资源不做换行转换
*.ico binary
*.png binary
*.exe binary
